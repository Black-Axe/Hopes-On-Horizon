import React, {useState,} from 'react'

export default function Animals() {
    const [animals, getAnimals] = useState(null);
    return (
        <div>
            animals component
        </div>
    )
}
